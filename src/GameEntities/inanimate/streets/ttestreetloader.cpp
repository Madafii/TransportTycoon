#include "ttestreetloader.h"

TTEStreetLoader::TTEStreetLoader(const QString filePath, const quint16 typeSize, const quint16 orientationSize)
    :TTEImageSetLoader(filePath, typeSize, orientationSize)
{}
