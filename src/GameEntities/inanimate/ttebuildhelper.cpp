#include "ttebuildhelper.h"

bool TTEBuildHelper::buildHereAllowed(const TTETileType &buildOn, const TTERailType &buildWhat)
{
    // TODO: implement later for now just allow building on everything
    return true;
}

bool TTEBuildHelper::buildHereAllowed(const TTETileType &buildOn, const TTETileType &buildWhat)
{
    // TODO: implement later for now just allow building on everything
    return true;
}
