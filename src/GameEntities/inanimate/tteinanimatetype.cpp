#include "tteinanimatetype.h"

// FIXME: have to specify because of some linking error i guess. Maybe should try to fix it.
// template<> TTEInanimateType<RAIL_TYPE, RAIL_ORIENTATION>::TTEInanimateType(RAIL_TYPE type, RAIL_ORIENTATION orientation, const QPixmap &image)
//     : type(type), orientation(orientation), image(image)
// {}

// template<> TTEInanimateType<TILE_TYPE, TILE_ORIENTATION>::TTEInanimateType(TILE_TYPE type, TILE_ORIENTATION orientation, const QPixmap &image)
//     : type(type), orientation(orientation), image(image)
// {}
